# 2026-06-20

## Sheet2 UPC Product Catalog Replacement

### What Changed

- Extracted `手机ipad型号查询汇总` from `/Users/lilemac/Downloads/6.16-6.30.xlsx`.
- Normalized the sheet into UPC product catalog rows for the running system.
- Cleared the existing product catalog tables on the VPS and imported the extracted Sheet2 data.
- Created a production database backup before the replacement.

### Import Summary

- Source rows with data: 193.
- Duplicate UPC values found: 1.
- Duplicate policy: keep the last occurrence from the sheet.
- Imported products: 192.
- Imported UPC mappings: 192.
- Backup created before import: `backups/wms-postgres-20260620-030342.sql.gz`.

### Touched System Data

- Cleared and replaced: `products`, `product_upcs`.
- Preserved: users, roles, permissions, customers, warehouses, settings, audit logs, backups, and server `.env.production`.
- Reference safety check before clearing: `inbound_items = 0`, `inventory_items = 0`, and `exception_records = 0`.

### Usage Logic

- Imported SKUs use `UPC-<upc>` so each product can be traced directly to the source UPC.
- `name` comes from the sheet's `物品名称` column.
- `brand` defaults to `Apple`.
- `requiresImei` is true for iPhone rows and false for iPad, MacBook, Watch, AirTag, Apple TV, and other non-iPhone rows.
- Category, model, color, and capacity are best-effort parsed from the source product name for easier filtering.

### Verification

- Production health check passed: `http://24.199.87.181/api/v1/health`.
- Database counts after import: `products = 192`, `product_upcs = 192`.
- Duplicate UPC check after import returned no duplicate UPC groups.

## UPC Pagination And Database Access Notes

### What Changed

- Added visible pagination controls to the UPC product library page.
- Added a page-size selector with `20`, `50`, and `100` rows per page.
- Added visible range text such as `第 1-20 条 / 共 192 条` so UPC catalog counting is easier.
- Documented direct server database access through SSH and `docker compose exec postgres`.

### Touched Modules

- `apps/web/src/pages/upc-library/page.tsx`: now sends `page` and `pageSize` to the existing paginated products API and renders page controls.
- `apps/web/src/styles/globals.css`: adds shared pagination bar styling.
- `README.md`: documents safe direct database access for the current test server.
- `docs/deployment/01-low-cost-vps-deployment.md`: documents direct PostgreSQL access without exposing the database port publicly.

### Usage Logic

1. Open UPC 商品库.
2. Choose `20`, `50`, or `100` rows per page.
3. Use 上一页 / 下一页 to move through the catalog.
4. For manual database changes, SSH to `root@24.199.87.181`, enter `/opt/wms-scan`, create a backup, then use `psql` inside the `postgres` container.
