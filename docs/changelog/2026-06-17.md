# 2026-06-17

## What Changed

- Finished roadmap phase 7 for backend inbound scanning workflows.
- Finished roadmap phase 8 for backend inbound record lookup workflows.
- Finished roadmap phase 9 for backend customer inventory workflows.
- Finished roadmap phase 10 for backend outbound packing workflows.
- Added inbound draft creation, draft lookup, UPS scan validation, preview item creation, preview item removal, draft clearing, draft confirmation, inbound record list, inbound record detail, batch item lookup, and inbound export-preview APIs under `apps/api/src/modules/inbound`.
- Added inventory customer summary, product/SKU summary, product item expansion, item list, item detail, outbound availability, and inventory export-preview APIs under `apps/api/src/modules/inventory`.
- Added outbound box creation, box listing, box detail, customer-scoped available inventory lookup, add item, remove item, clear box, and seal box APIs under `apps/api/src/modules/outbound`.
- Added inbound service rules for locked customer enforcement, active warehouse checks, active UPC/product matching, product `requiresImei` enforcement, IMEI/Serial validation, duplicate identity handling, unmatched UPC exception preview rows, and confirmed-record filtering.
- Added inventory service rules for customer-scoped summaries, status count aggregation, search across UPC/product/IMEI/Serial/UPS, `IN_STOCK` outbound availability, and item-level response shaping.
- Added outbound service rules for active customer and warehouse checks, warehouse-unique box numbers, same-customer and same-warehouse packing, `IN_STOCK`-only packing, duplicate outbound-box item prevention, open-box-only mutation, and non-empty seal confirmation.
- Added inbound confirmation transaction logic that creates inventory, updates inbound rows, marks duplicate rows as exceptions, closes the batch, and writes `INBOUND_CONFIRM` audit logs.
- Added outbound packing transaction logic that moves inventory to `PACKED` when added, restores inventory to `IN_STOCK` when removed or cleared, seals boxes, and writes `OUTBOUND_BOX_SEAL` audit logs.
- Added phase-eight record filters for time range, customer, warehouse, batch, UPS, UPC, IMEI, Serial, inbound row status, and linked inventory status.
- Added `selectableForCustomerChange` to inbound record rows so the batch customer-change page can reuse record selections.
- Added `POST /inbound/records/export-preview` so the detail-download and reports modules can reuse normalized inbound filters later.
- Added `GET /inventory/export-preview` so the detail-download and reports modules can reuse normalized inventory filters later.
- Added API, product, and database transaction documentation for inbound scanning, API documentation for inbound records, API/product documentation for inventory, and API/product/database documentation for outbound packing.
- Updated the backend roadmap and root README so project status reflects completed backend phases 1 through 10.
- Preserved prior phase 1 through 6 work: unified API errors, request IDs, Swagger groups, pagination DTO, Prisma schema and seed baseline, auth, users, roles, permissions, warehouses, settings, customers, and UPC product library.

## Files Or Modules Touched

- Inbound module: `apps/api/src/modules/inbound`.
- Inbound DTOs: `apps/api/src/modules/inbound/dto`.
- Inbound tests: `apps/api/src/modules/inbound/tests/inbound.service.spec.ts`.
- Inventory module: `apps/api/src/modules/inventory`.
- Inventory DTOs: `apps/api/src/modules/inventory/dto`.
- Inventory tests: `apps/api/src/modules/inventory/tests/inventory.service.spec.ts`.
- Outbound module: `apps/api/src/modules/outbound`.
- Outbound DTOs: `apps/api/src/modules/outbound/dto`.
- Outbound tests: `apps/api/src/modules/outbound/tests/outbound.service.spec.ts`.
- Project entry docs: `README.md`.
- API docs: `docs/api/08-inbound-scan.md`, `docs/api/09-inbound-records.md`, `docs/api/10-inventory.md`, `docs/api/11-outbound-packing.md`, `docs/api/README.md`.
- Product docs: `docs/product/05-inbound-rules.md`, `docs/product/06-inventory-rules.md`, `docs/product/07-outbound-rules.md`.
- Database docs: `docs/database/05-inbound-inventory-transaction.md`, `docs/database/06-outbound-transaction.md`, `docs/database/README.md`.
- Architecture roadmap: `docs/architecture/backend-implementation-roadmap.md`.
- Changelog: `docs/changelog/2026-06-17.md`.
- Static prototype reference: `docs/ui-prototype/original-html` was not modified.

## Module Purpose

- `apps/api/src/modules/inbound/inbound.controller.ts`: exposes phase-seven and phase-eight inbound HTTP endpoints behind `inbound.manage`.
- `apps/api/src/modules/inbound/inbound.service.ts`: owns inbound business rules, normalization, scan validation, draft lifecycle, preview responses, record filtering, detail responses, batch item responses, and export-preview payloads.
- `apps/api/src/modules/inbound/inbound.repository.ts`: owns Prisma reads/writes, record filter construction, batch item lookup, export count lookup, and the transaction that confirms drafts into inventory.
- `apps/api/src/modules/inbound/dto`: owns request contracts for draft creation, UPS scan validation, preview item creation, and record filtering.
- `apps/api/src/modules/inventory/inventory.controller.ts`: exposes phase-nine inventory HTTP endpoints behind `inventory.read`.
- `apps/api/src/modules/inventory/inventory.service.ts`: owns inventory customer scope checks, status aggregation, SKU summary shaping, item filtering, outbound availability rules, and export-preview payloads.
- `apps/api/src/modules/inventory/inventory.repository.ts`: owns Prisma inventory reads, product aggregation, item detail includes, and reusable search/outbound where fragments.
- `apps/api/src/modules/inventory/dto`: owns request contracts for customer summary, product summary, item list, outbound availability, and export-preview filters.
- `apps/api/src/modules/outbound/outbound.controller.ts`: exposes phase-ten outbound packing HTTP endpoints behind `outbound.manage`.
- `apps/api/src/modules/outbound/outbound.service.ts`: owns outbound business rules, box lifecycle checks, customer and warehouse ownership validation, inventory status validation, response shaping, and inventory availability delegation.
- `apps/api/src/modules/outbound/outbound.repository.ts`: owns Prisma outbound reads/writes and transactions for add, remove, clear, and seal operations.
- `apps/api/src/modules/outbound/dto`: owns request contracts for box creation, available item filters, box filters, and item packing.
- `docs/api/08-inbound-scan.md`: documents phase-seven endpoint behavior and request examples.
- `docs/api/09-inbound-records.md`: documents phase-eight record filtering, detail, batch item, customer-change selection, and export-preview behavior.
- `docs/api/10-inventory.md`: documents phase-nine customer summary, SKU summary, item detail, outbound availability, and export-preview behavior.
- `docs/api/11-outbound-packing.md`: documents phase-ten box creation, available inventory, item packing, removal, clearing, sealing, and response behavior.
- `docs/product/05-inbound-rules.md`: documents customer lock, UPC, IMEI, Serial, UPS, confirmation, and delete rules.
- `docs/product/06-inventory-rules.md`: documents inventory ownership, status meaning, SKU summary, IMEI/Serial details, outbound availability, and export filter rules.
- `docs/product/07-outbound-rules.md`: documents outbound customer ownership, warehouse ownership, selectable inventory, box lifecycle, inventory status changes, and audit requirements.
- `docs/database/05-inbound-inventory-transaction.md`: documents the draft-to-inventory transaction boundary and rollback expectations.
- `docs/database/06-outbound-transaction.md`: documents outbound add, remove, clear, and seal transaction boundaries and rollback expectations.

## Usage Logic

After local PostgreSQL is migrated and seeded, log in with the seeded development admin and call protected inbound endpoints with the returned bearer token.

Inbound scan pages can call:

```text
POST /api/v1/inbound/drafts
GET /api/v1/inbound/drafts/:id
POST /api/v1/inbound/drafts/:id/ups
POST /api/v1/inbound/drafts/:id/items
DELETE /api/v1/inbound/drafts/:id/items/:itemId
DELETE /api/v1/inbound/drafts/:id/items
POST /api/v1/inbound/drafts/:id/confirm
```

Inbound record, batch customer-change, and detail-download pages can call:

```text
GET /api/v1/inbound/records
GET /api/v1/inbound/records/:id
GET /api/v1/inbound/records/:id/items
POST /api/v1/inbound/records/export-preview
```

Example record filters:

```json
{
  "customerId": "customer-1",
  "warehouseId": "warehouse-1",
  "status": "CONFIRMED",
  "inventoryStatus": "IN_STOCK",
  "upsTrackingNo": "1Z999AA10123456784",
  "upc": "194253149189",
  "imei": "356789012345678",
  "dateFrom": "2026-06-01T00:00:00.000Z",
  "dateTo": "2026-06-30T23:59:59.999Z"
}
```

`GET /inbound/records` returns normalized row data, linked inventory status, exception summaries, and `selectableForCustomerChange`.

`POST /inbound/records/export-preview` returns the estimated row count and a reusable report payload so the future reports module can create export jobs with the same filter semantics.

`POST /inbound/drafts/:id/confirm` rechecks duplicates inside a database transaction. Only confirmable rows create `inventory_items`; duplicate rows are marked `EXCEPTION`; the batch is marked `CONFIRMED`; and an `INBOUND_CONFIRM` audit log is written.

Customer inventory, outbound packing, detail-download, and dashboard pages can call:

```text
GET /api/v1/inventory/customer-summary
GET /api/v1/inventory/products
GET /api/v1/inventory/products/:productId/items
GET /api/v1/inventory/items
GET /api/v1/inventory/items/:id
GET /api/v1/inventory/available-for-outbound
GET /api/v1/inventory/export-preview
```

Example inventory filters:

```json
{
  "customerId": "customer-1",
  "warehouseId": "warehouse-1",
  "status": "IN_STOCK",
  "upc": "194253149189",
  "imei": "356789012345678",
  "search": "iPhone 16 Pro"
}
```

`GET /inventory/customer-summary` returns customer status totals and SKU count. `GET /inventory/products` returns SKU/product-level counts. `GET /inventory/items` returns item-level IMEI/Serial rows. `GET /inventory/available-for-outbound` requires `customerId` and forces `IN_STOCK`, so exception, packed, outbound, and voided rows are not selectable for outbound packing.

Outbound packing pages can call:

```text
POST /api/v1/outbound/boxes
GET /api/v1/outbound/boxes
GET /api/v1/outbound/boxes/:id
GET /api/v1/outbound/available-items
POST /api/v1/outbound/boxes/:id/items
DELETE /api/v1/outbound/boxes/:id/items/:itemId
DELETE /api/v1/outbound/boxes/:id/items
POST /api/v1/outbound/boxes/:id/seal
```

Example outbound box creation:

```json
{
  "customerId": "customer-1",
  "warehouseId": "warehouse-1",
  "boxNo": "BOX-20260617-001",
  "notes": "Packing lane A"
}
```

`GET /outbound/available-items` requires `customerId` and reuses inventory availability logic. `POST /outbound/boxes/:id/items` only accepts `IN_STOCK` inventory from the same customer and warehouse as the open box, then marks it `PACKED`. Removing or clearing an open box returns affected inventory to `IN_STOCK`. `POST /outbound/boxes/:id/seal` requires at least one item, seals inside a database transaction, and writes an `OUTBOUND_BOX_SEAL` audit log.

## Where To Modify This Area Later

- Add new inbound request fields in `apps/api/src/modules/inbound/dto`, then map them in `apps/api/src/modules/inbound/inbound.service.ts`.
- Change draft lifecycle rules in `apps/api/src/modules/inbound/inbound.service.ts`.
- Change transaction behavior in `apps/api/src/modules/inbound/inbound.repository.ts`.
- Change inbound record filtering in `apps/api/src/modules/inbound/dto/list-inbound-records-query.dto.ts`, `apps/api/src/modules/inbound/inbound.service.ts`, and `apps/api/src/modules/inbound/inbound.repository.ts`.
- Change inventory filtering in `apps/api/src/modules/inventory/dto/list-inventory-items-query.dto.ts`, `apps/api/src/modules/inventory/inventory.service.ts`, and `apps/api/src/modules/inventory/inventory.repository.ts`.
- Change SKU summary behavior in `apps/api/src/modules/inventory/inventory.repository.ts` and response shaping in `apps/api/src/modules/inventory/inventory.service.ts`.
- Change outbound availability rules in `apps/api/src/modules/inventory/inventory.service.ts` and `InventoryRepository.toOutboundAvailableWhere`.
- Change outbound box request fields in `apps/api/src/modules/outbound/dto`, then map them in `apps/api/src/modules/outbound/outbound.service.ts`.
- Change outbound packing lifecycle rules in `apps/api/src/modules/outbound/outbound.service.ts`.
- Change outbound transaction behavior in `apps/api/src/modules/outbound/outbound.repository.ts`.
- Add customer or warehouse data-scope permission rules in the service or a dedicated policy layer before repository queries.
- Add new exception types in `apps/api/prisma/schema.prisma`, `packages/shared/src/enums`, and related inbound service branches.
- Reuse `GET /inbound/records` for the inbound records page, batch customer-change selection, and report/export filters.
- Reuse confirmed `inventory_items` from phases 7 and 8 when implementing customer inventory and outbound phases.
- Reuse `GET /inventory/available-for-outbound` when implementing outbound packing.
- Keep `docs/api/08-inbound-scan.md`, `docs/api/09-inbound-records.md`, `docs/product/05-inbound-rules.md`, and `docs/database/05-inbound-inventory-transaction.md` synchronized with future inbound behavior changes.
- Keep `docs/api/10-inventory.md` and `docs/product/06-inventory-rules.md` synchronized with future inventory behavior changes.
- Keep `docs/api/11-outbound-packing.md`, `docs/product/07-outbound-rules.md`, and `docs/database/06-outbound-transaction.md` synchronized with future outbound behavior changes.

## Verification

- Passed: API TypeScript check with bundled Node and `apps/api/node_modules/.bin/tsc --noEmit -p apps/api/tsconfig.json`.
- Passed: API full Jest suite with bundled Node from `apps/api` and `node_modules/.bin/jest --runInBand`.
- Passed: outbound module Jest test with bundled Node from `apps/api` and `node_modules/.bin/jest src/modules/outbound/tests/outbound.service.spec.ts --runInBand`.
- Passed: API ESLint check with bundled Node and `node_modules/.bin/eslint apps/api/src/**/*.ts`.
- Passed: Prettier check for touched API and documentation files.
- Not run: actual API server smoke test against PostgreSQL, because no database startup or data mutation was performed in this task.

## Safety Notes

- No real `.env` file was created.
- No real password, API key, production credential, or customer data was added.
- `node_modules` remains ignored and should not be committed.
- The original HTML prototype directory remains untouched.
