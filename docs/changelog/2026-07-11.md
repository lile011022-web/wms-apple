# 2026-07-11

## 商品导出、入库扫码与装箱维护发布准备

- 将 2026-07-10 完成的入库页布局恢复、FedEx 9632 规则、商品编辑/安全删除/可重新导入导出、以及未封箱货物编辑整理为隔离部署分支。
- 前端主要维护位置为 `apps/web/src/pages/inbound-scan`、`apps/web/src/pages/upc-library/page.tsx`、`apps/web/src/pages/outbound-packing/page.tsx` 和 `apps/web/src/styles/globals.css`。
- 后端主要维护位置为 `apps/api/src/modules/products` 和 `apps/api/src/modules/outbound`；扫码共享规则位于 `packages/shared/src/validators`。
- 由于当前 Mac 的 VPN 链路无法完成 SSH banner 握手，发布改为通过 GitHub HTTPS 隔离分支下载，避免使用公开临时文件服务。

### 验证

- 共享扫码规则 8 条测试通过。
- 商品和出库服务 30 条测试通过。
- API 与 Web TypeScript 类型检查通过。
- Web 生产构建通过；仅保留已知的大体积 chunk 警告。

### 发布逻辑

1. 服务器从隔离分支的 GitHub HTTPS 归档下载代码。
2. 同步时必须保留 `.env.production`、`backups/`、`uploads/` 和 Docker volumes。
3. 替换容器前运行 `infra/scripts/backup-postgres.sh`，然后部署 API/Web 并验证 `/api/v1/health`。
