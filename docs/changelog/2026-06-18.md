# 2026-06-18

## What Changed

- Finished roadmap phase 14 for backend Dashboard metrics and audit-log query APIs.
- Finished roadmap phase 15 for frontend API integration order and the first real API-backed settings page.
- Added Dashboard endpoints for summary, seven-day trends, open exception distribution, and top inbound customers.
- Added audit-log endpoints for recent operation logs and paginated filtered audit-log search.
- Added `audit-logs.read` to development seed permissions.
- Added API and product documentation for Dashboard and audit-log rules.
- Added unit tests for Dashboard metric rules and audit-log response/query behavior.
- Added a frontend API request helper that unwraps API envelopes, attaches Bearer tokens, and exposes a typed client error.
- Added frontend auth, workflow, and settings API helpers for the documented page connection order.
- Replaced the System Settings placeholder with a React Query page that reads warehouses/settings and saves all grouped settings.
- Added API documentation for the frontend integration baseline.

## Where To Modify Later

- Dashboard API surface: `apps/api/src/modules/reports/dashboard/dashboard.controller.ts`.
- Dashboard metric rules: `apps/api/src/modules/reports/dashboard/dashboard.service.ts`.
- Dashboard Prisma queries: `apps/api/src/modules/reports/dashboard/dashboard.repository.ts`.
- Dashboard request filters: `apps/api/src/modules/reports/dashboard/dto/dashboard-query.dto.ts`.
- Audit-log API surface: `apps/api/src/modules/audit-logs/audit-logs.controller.ts`.
- Audit-log query filters and response shaping: `apps/api/src/modules/audit-logs/audit-logs.service.ts` and `apps/api/src/modules/audit-logs/dto/list-audit-logs-query.dto.ts`.
- Seed permissions: `apps/api/prisma/seed.ts`.
- API docs: `docs/api/15-dashboard-audit-logs.md`.
- Product rules: `docs/product/11-dashboard-rules.md`.
- Frontend API client: `apps/web/src/api/client.ts`.
- Frontend token storage: `apps/web/src/api/token-store.ts`.
- Frontend auth helpers: `apps/web/src/api/auth.ts`.
- Frontend integration order: `apps/web/src/api/integration-plan.ts`.
- Frontend workflow API facade: `apps/web/src/api/workflow.ts`.
- System settings API helpers: `apps/web/src/api/settings.ts`.
- System settings page: `apps/web/src/pages/system-settings/page.tsx`.
- Frontend layout styles: `apps/web/src/styles/globals.css`.
- Frontend integration API docs: `docs/api/16-frontend-api-integration.md`.
- Roadmap: `docs/architecture/backend-implementation-roadmap.md`.

## Touched Modules And Purpose

- `apps/api/src/modules/reports/dashboard`: owns Dashboard metric endpoints and source queries.
- `apps/api/src/modules/audit-logs`: owns audit-log writing, recent operation logs, and paginated audit-log lookup.
- `apps/api/prisma/seed.ts`: keeps local admin role aligned with new backend permissions.
- `docs/api`: describes request and response contracts for frontend integration.
- `docs/product`: defines business rules for metrics and audit-log visibility.
- `apps/web/src/api`: owns the browser-side API client, auth token handling, page integration order, and API helper functions.
- `apps/web/src/pages/system-settings`: owns the first real API-backed settings page, including grouped settings form state and save behavior.
- `apps/web/src/styles`: keeps the settings form layout responsive inside the existing dashboard shell.

## Usage Logic

Dashboard pages call:

```text
GET /api/v1/dashboard/summary
GET /api/v1/dashboard/trends
GET /api/v1/dashboard/exception-distribution
GET /api/v1/dashboard/top-inbound-customers
```

Each Dashboard endpoint accepts optional `warehouseId` and requires `dashboard.read`.

Audit-log pages call:

```text
GET /api/v1/audit-logs/recent
GET /api/v1/audit-logs
```

`GET /audit-logs` supports pagination, search, action/resource/operator/request/date filters, and requires `audit-logs.read`.

Frontend pages use `request<T>()` from `apps/web/src/api/client.ts` so all successful responses are unwrapped from the backend API envelope. When a user has tokens in local storage, the client sends the access token as a Bearer token.

The System Settings page calls:

```text
GET /api/v1/warehouses?isActive=true
GET /api/v1/settings
PATCH /api/v1/settings
```

Operators edit default warehouse, scan rules, exception creation rules, notification switches, and retention days in one page. Saving sends the grouped settings payload back to `PATCH /settings`; the backend still owns permission checks, validation ranges, audit logging, and default warehouse existence checks.
